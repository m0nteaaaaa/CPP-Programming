//Program to display all Assignment Operators
#include<iostream>
using namespace std;
int main(){

    // = equal to
    // == comparing
    // += increment a = a + 1 or a+=1 
    // -= decrement a = a - 1 or a-=1
    // /= divide equal to a = a / 1 or a/=1
    // %= modulo qual to a = a % 1 or a%=1
    return 0
}